<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Main app theme - follows system light/dark automatically -->
    <style name="Theme.Reminder" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent_purple</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_purple</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/colorBackground</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:timePickerMode">spinner</item>

        <!-- Light theme surface colors -->
        <item name="android:windowBackground">@color/light_background</item>
        <item name="colorSurface">@color/light_surface</item>
        <item name="colorOnSurface">@color/light_text_primary</item>
    </style>

    <!-- TimePicker styles -->
    <style name="TimePickerTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:timePickerMode">spinner</item>
        <item name="colorPrimary">@color/accent_purple</item>
        <item name="colorAccent">@color/accent_purple</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/TimePickerButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/TimePickerButtonStyle</item>
    </style>

    <style name="TimePickerButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/accent_purple</item>
    </style>

</resources>